---
title: 'Custom checks'
---

By default, Dime generates verification for every test you create.
However, you can also add custom text checks for additional
flexibility.

To add content checks, click the "checks" option:

TODO: screenshot

Now, clicking text elements on the page will add assertions for those
text values. Click "actions" to go back to standard page actions.